# cppcryptfs-cracker

A simple program that attempts to find a cppcryptfs volume password via brute force. This is really only useful if you forgot a few characters from the beginning or the end of the password. This program might not work if the password has certain special characters.

## Requirements

Python 2.x (for brute to work)

brute (pip install brute)
